// Standalone PDF build of the demo — used only by scripts/build-pdfs.sh.
// (astro-typst never sees this file; it is not imported by any page.)

#set page(paper: "a4", margin: 2cm)
#include "demo.typ"
